# Fixtures are generated byte-for-byte by scripts/build-fixtures.mjs and are
# assertions in their own right. Formatting them rewrites what they test: the
# HTML-instead-of-XML fixture stops being malformed once prettier self-closes
# its void elements, and the parser guard it exists to exercise goes untested.
test/fixtures/
